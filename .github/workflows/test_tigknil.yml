name: CI

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v2
      - name: Run ShellCheck
        uses: bewuethr/shellcheck-action@v2.1.0
      - name: test_tigknil
        run: ./test_tigknil
